{
  "buildCommand": "npx prisma generate && npx prisma migrate deploy && npm run build -w frontend",
  "installCommand": "npm install",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
